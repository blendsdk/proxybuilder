# Generated Project Configuration

> **Auto-generated by `analyze_project`**
> **Project:** nginx-proxy
> **Type:** CLI tool (monorepo)

Save this content to `.clinerules/project.md` in your project root,
then review and adjust the values as needed.

---

## 🚨 MANDATORY: Load CodeOps Rules Before Any Work

**Before ANY planning or implementation, the AI agent MUST load these rules
using the codeops-mcp tools:**

1. `get_rule("agents")` — Load agent behavior rules **(REQUIRED FIRST)**
2. `get_rule("code")` — Load coding standards
3. `get_rule("testing")` — Load testing workflows
4. `get_rule("git-commands")` — Load git commit protocols

These rules are **mandatory** and must be consulted before every task.
**Do NOT skip this step. Do NOT proceed without reading these documents.**

---

## Project Overview

- **Name:** nginx-proxy
- **Description:** An nginx reverse proxy configuration builder CLI tool (`@truesoftware/proxybuilder`). Generates nginx proxy configs, manages SSL certificates via Let's Encrypt, and handles domain-based reverse proxy setups.
- **Type:** CLI tool (monorepo)

## Toolchain

- **Language(s):** TypeScript
- **Framework(s):** yargs (CLI), shelljs (shell commands)
- **Package Manager:** yarn (with Lerna for monorepo management)
- **Test Framework:** None (no tests currently)
- **Structure:** Monorepo (Lerna + Yarn Workspaces)

**Manifest files found:** package.json, lerna.json, tsconfig.json

## Commands

All commands assume execution from the project root. Prefix all shell commands with `clear &&`.

### Build

```bash
clear && yarn build
```

### Test

```bash
# No tests currently — test script exits with 0
clear && yarn test
```

### Verify (before commit)

```bash
# Full verification — run this before any git commit
clear && yarn build
```

## Project Structure

### Type: Monorepo

### Directory Layout

```
packages/
  proxybuilder/             # Main CLI package (@truesoftware/proxybuilder)
    src/
      index.ts              # CLI entry point (yargs command router)
      builder.ts            # ProxyBuilder class — core proxy config logic
      lib.ts                # Utility functions (logging, file ops, templates)
      types.ts              # TypeScript interfaces
      commands/             # yargs command handlers
        create.ts           # Create proxy config for a domain
        renew.ts            # Renew SSL certificates
        revoke.ts           # Revoke SSL certificates
    resources/              # Nginx config templates (.conf files)
      nginx.conf            # Main nginx config template
      proxy.conf            # Proxy settings template
      site.conf             # Site config template
      site-ssl.conf         # SSL config template
      site-upstream.conf    # Upstream config template
      letsencrypt.conf      # Let's Encrypt config template
      initial.conf          # Initial config template
      site-custom.conf      # Custom site config template
      site-general.conf     # General site config template
      site-log.conf         # Logging config template
      site-main.conf        # Main site config template
      site-security.conf    # Security config template
```

## Coding Conventions

### Naming

- **Files:** kebab-case (e.g., `site-ssl.conf`, `builder.ts`)
- **Classes:** PascalCase (e.g., `ProxyBuilder`)
- **Interfaces:** PascalCase with `I` prefix (e.g., `ITargetFolder`)
- **Functions/Methods:** camelCase (e.g., `createSiteProxy`, `renderTemplate`)
- **Constants:** camelCase (no UPPER_SNAKE_CASE detected)

## Git & Commit Conventions

### Commit Scope

```
# Monorepo — use package name as scope:
# feat(proxybuilder): description
```

### Branch Strategy

- **Main branch:** `main`
- **Feature branches:** `feature/[name]`

## Special Rules (Project-Specific)

```
- CLI binary entry point: packages/proxybuilder/dist/index.js
- Nginx config templates use custom template rendering (renderTemplate in lib.ts)
- The project manages real infrastructure (nginx, SSL certs) — changes must be tested carefully
- Lerna version: 1.0.62 (conventional commits for versioning)
```

## Cross-References

The generic rule files that read this `project.md`:

- **make_plan.md** — Uses verify command, file paths, commit scope
- **code.md** — Uses language conventions, architecture rules
- **testing.md** — Uses test commands, test locations, test framework
- **git-commands.md** — Uses commit scope, verify command
- **agents.md** — Uses shell commands, verify command
- **plans.md** — Uses task file path patterns
